import {AppSettings} from '../../global/settings';
import {FirebaseStorage} from '../../global/firebaseStorage';


export const Settings = new AppSettings();
export const AppStorage = new FirebaseStorage();

